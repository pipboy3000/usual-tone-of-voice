Place a bundled whisper.cpp binary here (optional).
Expected filename: whisper-cli

If present, the app will prefer this bundled binary over system paths.

Menu bar icon override (optional):
- Add a template image named "menubar-icon" in this folder.
- Supported formats: pdf, png, tiff, icns.
- The icon is used when there is no error state.
